# Tests

This directory contains the automated test suite for Challenge Tracker.

Run all tests from the project root:

```bash
npm run test
```

Run subsets:

```bash
npm run test:unit
npm run test:integration
```